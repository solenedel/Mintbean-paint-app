import React from 'react';

const Nav = ({ className }) => {
  return ( 
    <nav className={className}>
      <div id="nav-flexbox">
        <div id="logo">Café Finder</div>
      </div>
    </nav>
   );
}
 
export default Nav;